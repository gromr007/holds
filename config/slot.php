<?php

declare(strict_types=1);

return [

    /*

    |--------------------------------------------------------------------------
    | Настройки Spinlock для кэширования слотов
    |--------------------------------------------------------------------------

    |
    | Управление поведением микросекундного замка при защите от Cache Stampede.
    |
    */
    'spinlock' => [
        // Максимальное время ожидания прогрева кэша другими потоками (в миллисекундах)
        'max_wait_ms'        => (int) env('BOOKING_SPINLOCK_MAX_WAIT_MS', 3000),

        // Шаг циклической проверки освобождения замка (в микросекундах)
        'spin_retry_us'      => (int) env('BOOKING_SPINLOCK_SPIN_RETRY_US', 5000),

        // Искусственная задержка тяжелой БД для локальных тестов (в микросекундах)
        'simulated_delay_us' => (int) env('BOOKING_SIMULATED_DELAY_US', 30000),
    ],

];
